apiVersion: v1
kind: Service
metadata:
  name: python-info-service
  labels:
    app.kubernetes.io/name: python-info-service
spec:
  type: NodePort
  selector:
    app: python-info-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
