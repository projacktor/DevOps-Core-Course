[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = True
remote_user = ubuntu
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
become_user = root